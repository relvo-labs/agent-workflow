# v0.1 Draft foundation

Issue: https://github.com/relvo-labs/agent-workflow/issues/1
Base: `89f5395e6f30fb4f7e7d3697f732a5785855e4bd`
Branch: `feat/v0.1-skills-foundation`

Classification: bounded initial public-contract draft; one writer, no runtime or deployment.
Write scope: this repository's original skills, contracts, validator, fixtures, tests,
documentation and generated product diagram. No global configuration or private sources.

Dependency graph:
1. Freeze workflow/role semantics and versioned contracts.
2. Implement offline validation, fixtures and single-responsibility skills against contracts.
3. Generate diagram from workflow data; validate links, metadata and determinism.
4. Run local gates, inspect source/provenance; commit/push coherent Draft checkpoint.
5. Exercise quickstart from clean environment, finish sensitive/license checks, report exact head.
6. Independent fresh review by coordinating owner; human decides Ready/merge separately.

Stop after one consolidated repair if a remaining blocker cannot be resolved within scope.
Provider probes, native adapters, distributed locking and hosted Actions are excluded.
No provider/model choice in this run establishes a product default.

## Bounded pre-release contract repair

- Permit a generic provider/session identity without development fields or candidate
  SHA; keep the complete development binding and SHA requirements conditional and
  synchronized in standalone/embedded schemas. Preserve all record correlations.
- Reject acknowledged handoffs contradicted by a submitted running prior session,
  even without write paths and under a blocked outcome; retain stopped controls.
- Add precise regression tests and a delivered non-repository coordinator fixture;
  run reproducers, unit/schema/fixture/adoption gates, diagram and diff checks.
- Freeze one repair commit on the same Draft PR for independent delta review.
  No runtime, provider probes, dependency/profile changes or expanded feature scope.
